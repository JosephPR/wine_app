<!DOCTYPE html>
<html lang="en" dir="ltr">
<head>
  <meta charset="utf-8">
  <title>Recommendations</title>
</head>
  <body>
    <div class="container">
      <h1>
        <center>
          <br>
          <%=link_to "Get New Recommendations", new_recommendation_path, :style => 'color: rgb(107, 35, 25)'%>
      
        </center>
        </h1>
        <%= form_tag "/recommendations", method: :get do %>
        <%= text_field_tag :search_term%>
        <%= submit_tag "Search", :class => 'btn btn-outline-light btn-sm', :style => 'color: rgb(107, 35, 25)', :id => 'wine-search' %>
      <% end %>
    </div>
  <div class="container svg">
    <div  class="row">
      <div class="col-lg  rec">
        <hr id="hrmain">
          <% @recommendations.each do |recommendation| %>
            <p><%=link_to recommendation.name, varietals_path(@varietal), :id => "namelink" %></p>
            <p>Vintage: <%= recommendation.year %></p>
            <p>Region: <%= recommendation.region %></p>
            <p>Global Wine Score: <%= recommendation.score %></p>
            <p><%=link_to "Wine Details", recommendation.url, target: :_blank %></p>
          <% end %>
        </hr>
      </div>
    </div>
  </div>
</body>
</html>
